% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_ipd_ad_gaussian.R
\name{fit_ipd_ad_gaussian}
\alias{fit_ipd_ad_gaussian}
\title{Fit Application-style continuous IPD + AD meta-analysis}
\usage{
fit_ipd_ad_gaussian(
  ipd,
  ad_type1 = NULL,
  ad_type2 = NULL,
  ad_type3 = NULL,
  burnin = 10000L,
  mainrun = 10000L,
  mu_sig2 = 20,
  tau_sig2 = 20,
  mu0 = NULL,
  Sigma0 = NULL,
  nu0 = 0.1,
  psi0 = 0.1,
  step_type1 = 0.2,
  step_type2 = 0.2,
  step_type3 = 0.1,
  step_sig2 = 2,
  step_alpha = 0.001,
  step_tau = 0.2,
  tau_update_after = 1000L,
  type1_coef_index = 7L,
  theta_init = NULL,
  mu_init = NULL,
  Sigma_init = NULL,
  sig2_init = NULL,
  seed = 117L,
  verbose = TRUE,
  progress_every = 100L
)
}
\arguments{
\item{ipd}{IPD via \code{\link[=as_application_ipd]{as_application_ipd()}}.}

\item{ad_type1}{Optional data frame with columns \code{beta_hat}, \code{sqrt_hat_V}.
Type1 bridges to coefficient 7 (\code{bmi_cat3.trt} in Application coding).}

\item{ad_type2}{Optional data frame with columns
\verb{p_01,p_02,p_03,p_11,p_12,p_13,beta_hat,sqrt_hat_V}.}

\item{ad_type3}{Optional list with Type3 components:
\code{betahat}, \code{se} (\verb{J x 6}), \code{use_drm} (logical), \code{hat_tau}, \code{hat_gamma_tau},
\code{Design_X}, \code{nu_X}, \code{threshold}, \code{DRM_X} (each length-\code{J} lists / vectors).
Each \code{DRM_X[[k]]} needs columns \code{int}, \code{DRM_X_type3}, \code{a_n}.}

\item{burnin, mainrun}{MCMC lengths. Defaults \code{10000}.}

\item{mu_sig2, tau_sig2}{Truncated-normal prior for residual variances.}

\item{mu0, Sigma0}{Prior for \code{mu}.}

\item{nu0, psi0}{Inverse-Wishart hyperparameters.}

\item{step_type1, step_type2, step_type3, step_sig2, step_alpha, step_tau}{MH scales.}

\item{tau_update_after}{Start tau MH only after this iteration (official: 1000).}

\item{type1_coef_index}{Coefficient index bridged by Type1. Default \code{7}.}

\item{theta_init, mu_init, Sigma_init, sig2_init}{Starting values.}

\item{seed}{Default \code{117}. \code{NULL} leaves RNG unchanged.}

\item{verbose, progress_every}{Progress printing controls.}
}
\value{
A \code{bayesmetaipd_fit} with \code{posterior_mu}, \code{posterior_Sigma_diag},
\code{posterior_sig2} (matrix over IPD + Type3 studies), \code{call}, \code{settings}.
}
\description{
Implements \code{Code/Application_CodeOnly.R}: Gaussian IPD with study-specific
residual variances, plus Aggregate Data of three report types
(treatment-effect Type1, BMI-mixture Type2, threshold-probability Type3 with
optional density-ratio adjustment).
}
\details{
Real I-WIP Application CSVs are not bundled. Provide prepared objects, or use
\code{\link[=example_application_data]{example_application_data()}} for a small synthetic example.
}
\examples{
toy <- example_application_data()
fit <- fit_ipd_ad_gaussian(
  ipd = toy$ipd,
  ad_type1 = toy$ad_type1,
  ad_type2 = toy$ad_type2,
  ad_type3 = toy$ad_type3,
  burnin = 2, mainrun = 3, verbose = FALSE,
  tau_update_after = 0L
)
colMeans(fit$posterior_mu)

}
