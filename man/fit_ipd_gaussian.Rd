% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_ipd_gaussian.R
\name{fit_ipd_gaussian}
\alias{fit_ipd_gaussian}
\title{Fit Application-style continuous IPD-only meta-analysis}
\usage{
fit_ipd_gaussian(
  ipd,
  burnin = 10000L,
  mainrun = 10000L,
  mu_sig2 = 20,
  tau_sig2 = 20,
  mu0 = NULL,
  Sigma0 = NULL,
  nu0 = 0.1,
  psi0 = 0.1,
  step_sig2 = 2,
  theta_init = NULL,
  mu_init = NULL,
  Sigma_init = NULL,
  sig2_init = NULL,
  seed = 117L,
  verbose = TRUE,
  progress_every = 100L
)
}
\arguments{
\item{ipd}{IPD studies via \code{\link[=as_application_ipd]{as_application_ipd()}} format (list or data frame).}

\item{burnin}{Burn-in iterations. Default \code{10000}.}

\item{mainrun}{Retained iterations. Default \code{10000}.}

\item{mu_sig2, tau_sig2}{Truncated-normal prior mean/sd for each \code{sigma_l^2}
(support \verb{(0, Inf)}). Defaults \code{20}.}

\item{mu0}{Prior mean for \code{mu}. Default \code{0}.}

\item{Sigma0}{Prior covariance for \code{mu}. Default \code{100 * I}.}

\item{nu0}{Inverse-Wishart df. Default \code{0.1}.}

\item{psi0}{Inverse-Wishart scale multiplier. Default \code{0.1}.}

\item{step_sig2}{MH proposal SD for each \code{sigma_l^2}. Default \code{2}.}

\item{theta_init}{Optional \verb{J x p} starting coefficients.}

\item{mu_init}{Optional length-\code{p} starting \code{mu}.}

\item{Sigma_init}{Optional \verb{p x p} starting covariance.}

\item{sig2_init}{Optional length-\code{J} starting residual variances. Default \code{10}.}

\item{seed}{RNG seed. Default \code{117} (official Application script). \code{NULL}
leaves RNG unchanged.}

\item{verbose}{Print progress every \code{progress_every} iterations.}

\item{progress_every}{Progress print interval. Default \code{100}.}
}
\value{
A \code{bayesmetaipd_fit} list with \code{posterior_mu},
\code{posterior_Sigma_diag}, \code{posterior_sig2} (\verb{mainrun x J}), \code{call},
\code{settings}.
}
\description{
Implements the continuous IPD updates from
\code{Code/Application_CodeOnly.R}: Gaussian IPD likelihood, study-specific
residual variances with truncated-normal prior, Gibbs updates for study
coefficients, and hierarchical random effects for \code{mu} / \code{Sigma}.
}
\details{
This is the Application model (not Simulation Study 1). Real Application
I-WIP data are not redistributed; supply your own IPD.
}
\examples{
toy <- example_application_data()
fit <- fit_ipd_gaussian(toy$ipd, burnin = 5, mainrun = 10, verbose = FALSE)
colMeans(fit$posterior_mu)

}
