# Bayesian IPD / IPD+AD meta-analysis
#
# Install from GitHub:
#   install.packages("remotes")
#   remotes::install_github("yuanzhouu/bayesmetaipd")
#
# Quick start:
#   library(bayesmetaipd)
#   fit <- fit_ipd_ad_lm(formula = Y ~ X1 + X2, ipd = my_ipd, study = my_ipd$study)

#' @keywords internal
"_PACKAGE"
